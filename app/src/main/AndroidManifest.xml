<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.faogustavo.sample">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.KeyframeXmlActivity"/>
        <activity android:name=".view.KeyframeProgrammaticallyActivity"/>
        <activity android:name=".view.OrionProgramaticallyActivity"/>
        <activity android:name=".view.SizesActivity"/>
        <activity android:name=".view.RotationBiasActivity"/>
        <activity android:name=".view.CircleFabActivity"/>
    </application>

</manifest>